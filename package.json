{
  "name": "LabelMate",
  "version": "1.0.0",
  "description": "LabelMate is an automated GitHub workflow powered by ChatGPT that intelligently assigns labels to issues based on the content of their title and description.",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": " ./scripts/build.sh",
    "clean": "rm -rf ./dist"
  },
  "type": "commonjs",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "openai": "^4.76.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1"
  }
}
